<style>
  .prep-kitchen-shell { margin: 0; background: #0f111b; color: #eef2ff; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif; }
  .pk-app { min-height: 100vh; display: flex; }
  .pk-sidebar { width: 250px; padding: 18px; border-right: 1px solid #232743; background: #151a2f; display: flex; flex-direction: column; gap: 16px; }
  .pk-brand { font-weight: 700; font-size: 18px; color: #7dd3fc; }
  .pk-user { font-size: 13px; color: #c7d2fe; border: 1px solid #2a3058; border-radius: 10px; padding: 10px; }
  .pk-nav { display: flex; flex-direction: column; gap: 8px; }
  .pk-nav-item { color: #a5b4fc; text-decoration: none; padding: 9px 10px; border-radius: 10px; border: 1px solid transparent; }
  .pk-nav-item:hover { background: #1b2244; border-color: #37417f; }
  .pk-nav-item.active { background: #25306b; color: #eef2ff; border-color: #4f61d8; }
  .pk-content { flex: 1; padding: 20px; }
  .pk-flash { padding: 10px 12px; border-radius: 10px; margin-bottom: 10px; border: 1px solid; }
  .pk-flash-notice { background: rgba(16, 185, 129, 0.12); border-color: rgba(16, 185, 129, 0.35); }
  .pk-flash-alert { background: rgba(239, 68, 68, 0.12); border-color: rgba(239, 68, 68, 0.35); }
  .pk-card { border: 1px solid #2a3058; border-radius: 14px; background: #141933; padding: 14px; margin-bottom: 14px; }
  .pk-title { margin: 0 0 10px; font-size: 22px; }
  .pk-muted { color: #94a3b8; font-size: 13px; }
  .pk-table { width: 100%; border-collapse: collapse; }
  .pk-table th, .pk-table td { border-bottom: 1px solid #2a3058; text-align: left; padding: 8px; font-size: 14px; }
  .pk-kpis { display: grid; grid-template-columns: repeat(5, minmax(120px, 1fr)); gap: 10px; margin-bottom: 14px; }
  .pk-kpi { background: #171d3d; border: 1px solid #313a73; border-radius: 12px; padding: 10px; }
  .pk-actions { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 8px; }
  .pk-btn { background: #26357d; color: #eef2ff; border: 1px solid #3f56bf; border-radius: 8px; padding: 7px 10px; text-decoration: none; cursor: pointer; }
  .pk-btn-danger { background: #7f1d1d; border-color: #b91c1c; }
  .pk-form-row { display: flex; gap: 8px; margin-bottom: 8px; flex-wrap: wrap; }
  .pk-input, .pk-select { background: #0f1328; color: #e2e8f0; border: 1px solid #3a457f; border-radius: 8px; padding: 7px 8px; }
</style>
