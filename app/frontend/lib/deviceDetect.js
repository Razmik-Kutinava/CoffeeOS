/**
 * #37 — детекция ОС для CTA Wallet / Push.
 * RED stub: реализация в GREEN.
 *
 * @param {{
 *   userAgent?: string,
 *   platform?: string,
 *   maxTouchPoints?: number,
 *   hasWindow?: boolean
 * }} [env]
 * @returns {"ios"|"android"|"desktop"}
 */
export function getDeviceOS(_env = {}) {
  return undefined
}
