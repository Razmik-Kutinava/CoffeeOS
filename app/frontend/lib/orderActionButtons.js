/**
 * #41 — токены стиля и testid для OrderActionButtons (sticky panel).
 */

export const ACTION_CTA_STYLE = Object.freeze({
  background: "#ff6b35",
  color: "#ffffff",
  fontWeight: 600,
  heightPx: 44,
  borderRadius: "0.375rem",
  fontSizeRem: 0.75,
  actionsClass: "oab__actions",
  buttonClass: "oab__btn",
  actionsWidthRem: 11,
  actionsGapRem: 0.5
})

export const ORDER_ACTION_TEST_IDS = Object.freeze({
  root: "order-action-buttons",
  button: "order-action-btn"
})
