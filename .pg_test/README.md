# .pg_test

Локальный Postgres для тестов. Не коммитить.

> Для владельца и заказчика: что лежит в этой папке простыми словами. Агенту читать только если нужно понять зону — **не always**.

## Что внутри

- **`base/`** — Папка «base»: часть системы CoffeeOS. Смотри подпапки ниже
- **`global/`** — Папка «global»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_commit_ts/`** — Папка «pg commit ts»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_dynshmem/`** — Папка «pg dynshmem»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_logical/`** — Папка «pg logical»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_multixact/`** — Папка «pg multixact»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_notify/`** — Папка «pg notify»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_replslot/`** — Папка «pg replslot»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_serial/`** — Папка «pg serial»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_snapshots/`** — Папка «pg snapshots»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_stat/`** — Папка «pg stat»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_stat_tmp/`** — Папка «pg stat tmp»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_subtrans/`** — Папка «pg subtrans»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_tblspc/`** — Папка «pg tblspc»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_twophase/`** — Папка «pg twophase»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_wal/`** — Папка «pg wal»: часть системы CoffeeOS. Смотри подпапки ниже
- **`pg_xact/`** — Папка «pg xact»: часть системы CoffeeOS. Смотри подпапки ниже

## Зачем это нам

Чтобы быстро понимать структуру CoffeeOS без чтения кода.
