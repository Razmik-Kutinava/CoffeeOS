# Задача: Интерактивное табло баристы (CoffeeOS)

**ID:** B2.1 · **Источник:** заказчик, чат 2026-06  
**Статус:** ТЗ заведено 2026-06-10 · реализация `[ ]` · заказчик `[ ]` · **приоритет: блок 2 (сейчас)**

**Связано:** B1.1 (WS + push гостю) · B1.7 (витрина, оплата картой) · [B2.2](B2_2_barista_menu_create_merge.md) (меню + создать)

---

## CoffeeOS — scope (читаем перед PDF-текстом)

| Тема | Решение |
|------|---------|
| **Канал** | Web `/barista` (Rails + Hotwire/Turbo), **не** отдельные планшеты кухни |
| **Цепочка сейчас** | **Витрина** (карта) → **табло бариста** → **экран статуса гостя** (B1.1) |
| **Кухня / prep_kitchen** | В дорожной карте, **не акцент MVP** — после витрина↔табло |
| **PWA** | **Не MVP** — фаза 2 (конец) или фаза 3 |
| **Уведомления гостю** | Уже есть: `GuestOrderBroadcaster` + `OrderStatusPushNotifier` (B1.1) — web, не PWA |
| **Оплата** | Витрина: **только картой** (B1.7 BR-2). Бариста «Создать»: убрать наличные → **B2.2** |
| **Колонки табло** | `accepted` · `preparing` · `ready` (без отдельного «планшета упаковки») |
| **issued / выдача** | Вне MVP карточки, если заказчик не требует отдельно — кнопка «Выдать» на `ready` **как сейчас**, без нового экрана |
| **Брак / переделка / возврат / звук / defect_reasons** | **Фаза 2 (конец) или фаза 3** — в ТЗ заказчика есть, в MVP **не делаем** |

### Что уже есть в коде (baseline)

| Компонент | Файл |
|-----------|------|
| Kanban-табло | `app/views/barista/dashboard/index.html.erb` |
| Карточка заказа | `app/views/barista/dashboard/_order_card.html.erb` |
| Смена статуса | `Barista::OrderStatusUpdateService` → `GuestOrderBroadcaster` |
| Live-обновление табло | `Barista::OrderBoardBroadcaster` + Turbo Stream |
| Push гостю | `Shop::OrderStatusPushNotifier` (B1.1) |

**Baseline-скрин «до»:** [`screenshots/b21_barista_board_2026-06-10/barista_board_before.png`](../../artifacts/demo-feedback/screenshots/b21_barista_board_2026-06-10/barista_board_before.png)

---

## Проблема (заказчик)

В текущей системе табло бариста карточка заказа отображается в формате to-do list: бариста перемещает заказы по столбцам статусов. Нет крупной кнопки смены статуса, нет визуальной обратной связи через цвет карточки, нет явного отображения модификаторов рецепта (добавленные/убранные ингредиенты). Заказы не выстроены в очередь FIFO. При смене статуса клиент не всегда получает понятную обратную связь. Нет встроенных механик исключений (отмена, брак, переделка, возврат) в карточке.

---

## Решение (текст заказчика — без сокращений)

### Продуктовые требования

#### Блок 1. Интерактивная карточка заказа

- Каждая карточка заказа на табло представляет собой единый квадратный блок фиксированного размера.
- Карточка содержит:
  - Номер заказа и время создания.
  - Состав заказа с модификаторами: добавленные — капс + жирный + контрастный фон (**+ ПО КРЕПЧЕ**); удалённые — зачёркивание (**БЕЗ Сахара**).
  - Крупная кнопка смены статуса внизу (≥ 80 px, на всю ширину). Текст: «Оплачен» → **ГОТОВИТСЯ**; «Готовится» → **ГОТОВ**.
  - Цвет фона карточки по статусу:
    - «Принят» — серый;
    - «Оплачен» — синий;
    - «Готовится» — оранжевый;
    - «Готов» — зелёный.
- По нажатию кнопки карточка меняет цвет.

#### Блок 2. FIFO-очередь

- Заказы с веб-сайта в очереди FIFO по `created_at`.
- Ручное перетаскивание порядка **запрещено**.
- Порядок на экране: слева направо, сверху вниз.
- При смене статуса карточка уходит из колонки и появляется в следующей.

#### Блок 3. Уведомления клиенту

- При смене статуса баристой — уведомление на веб-сайт:
  - «Оплачен» → «Готовится»: «Ваш заказ начали готовить».
  - «Готовится» → «Готов»: «Заказ готов, забирайте!»
- WebSocket в реальном времени; при сбое — повтор до 3 раз, интервал 5 с.

#### Блок 4. Исключения (отмена, брак, переделка, возврат)

- **Отмена:** карточка блокируется, тёмно-серый, «СТОП! ЗАКАЗ ОТМЕНЁН», звук, кнопка «ПОДТВЕРДИТЬ ОТМЕНУ», черновик списания.
- **Брак / переделка:** кнопка «БРАК», справочник причин, списание со склада, возврат в начало очереди «Готовится», бейдж «ПЕРЕДЕЛКА».
- **Возврат:** через интерфейс брака + пометка «ВОЗВРАТ ОТ КЛИЕНТА».

### Технологические требования (заказчик)

- Карточка как компонент с `status`, `customizations`, `queue_position`.
- Очередь синхронизируется через WebSocket.
- `POST /api/orders/{id}/status` — у нас: `PATCH /barista/orders/:id` + `OrderStatusUpdateService`.
- Уведомления: WebSocket + fallback push (PWA — **не MVP**).
- `defect_reasons` — **не MVP**.
- Звук при отмене — **фаза 2/3**.
- `status_changed_at` с точностью до мс — через `OrderStatusLog`.

### User Story / Use Case / Corner cases

См. полный текст в переписке заказчика 2026-06 (основной конвейер, отмена, брак, двойной клик, офлайн, эскалация отмены 5 мин).

### Ограничения (заказчик)

- Не менять цепочку: Принят → Оплачен → Готовится → Готов.
- Не давать ручной порядок в очереди.
- Не давать выбор произвольного следующего статуса.
- Брак — не бариста, только менеджер/упаковщик (**фаза 2/3**).
- Причина брака только из справочника (**фаза 2/3**).

---

## Маппинг CoffeeOS: статусы ↔ колонки

| Колонка UI | `Order.status` | Цвет карточки (MVP) | Кнопка |
|------------|----------------|---------------------|--------|
| ACCEPTED | `accepted` | синий (# — уточнить в макете) | ГОТОВИТСЯ → `preparing` |
| PREPARING | `preparing` | оранжевый | ГОТОВ → `ready` |
| READY | `ready` | зелёный | Выдать → `issued` (как сейчас, без нового экрана) |

На табло попадают заказы **с витрины** (`source: mobile`) после оплаты и заказы **с ручного создания** бариста (после B2.2 — только безнал).

---

## MVP vs позже

| Функция | MVP B2.1 | Позже |
|---------|----------|-------|
| Квадратная карточка + цвет + большая кнопка | `[x]` | — |
| Модификаторы жирным / БЕЗ | `[x]` | — |
| FIFO по `created_at` в колонке | `[x]` | переделка в начало очереди |
| WS + push гостю при смене статуса | проверить B1.1 | PWA |
| Отмена на карточке (overlay) | упрощённо `[ ]` | звук, списание |
| Брак / переделка / возврат | — | фаза 2 конец / фаза 3 |
| `defect_reasons` | — | фаза 2/3 |
| Кухня / prep_kitchen табло | — | после витрина↔бариста |
| PWA | — | фаза 2/3 |

---

## Критерии приёмки (MVP)

| # | Критерий | Цель | MVP |
|---|----------|------|-----|
| 1 | Кнопка статуса ≥ 80 px, на всю ширину карточки | да | `[ ]` |
| 2 | Цвет карточки по статусу (accepted/preparing/ready) | да | `[ ]` |
| 3 | Модификаторы: добавленные выделены, убранные зачёркнуты | ≥ 98% | `[ ]` |
| 4 | FIFO в колонке по `created_at` | 100% | `[ ]` |
| 5 | Нет drag-and-drop смены порядка | да | `[ ]` |
| 6 | Смена статуса → обновление табло ≤ 1 с | ≤ 500 мс цель | `[ ]` |
| 7 | Смена статуса → WS на экран гостя ≤ 5 с | B1.1 | `[ ]` |
| 8 | Смена статуса → push (если включён) | B1.1 | `[ ]` |
| 9 | Заказ с витрины (карта) появляется на табло | да | `[ ]` |

---

## План реализации

| Этап | Содержание | Статус |
|------|------------|--------|
| **0** | Маппинг, baseline-скрин, артефакт `b21_stage0_*.json` | `[x]` |
| **1** | Карточка: layout, цвета, кнопка 80px, модификаторы | `[x]` 2026-06-10 |
| **2** | FIFO-сортировка в колонках, убрать ручной reorder | `[x]` 2026-06-11 |
| **3** | Связка статус → гость (WS/push), тексты уведомлений | `[ ]` |
| **4** | Отмена: overlay на карточке (без звука/списания в MVP) | `[ ]` |
| **5** | Тесты, MCP Fly, smoke, скрины «после» | `[ ]` |
| **6+** | Брак, переделка, возврат, звук, кухня, PWA | фаза 2/3 |

---

## Код (целевые файлы)

| Компонент | Файл |
|-----------|------|
| Табло | `app/views/barista/dashboard/index.html.erb` |
| Карточка | `app/views/barista/dashboard/_order_card.html.erb` |
| Стили | `app/assets/stylesheets/` (barista) |
| Статус | `app/controllers/barista/orders_controller.rb#update_status` |
| Сервис | `app/services/barista/order_status_update_service.rb` |
| Broadcast табло | `app/services/barista/order_board_broadcaster.rb` |
| Broadcast гостю | `app/services/shop/guest_order_broadcaster.rb` |
| Push | `app/services/shop/order_status_push_notifier.rb` |
| Fly smoke | `bin/b21_barista_board_fly_smoke.rb` *(создать на этапе 5)* |

---

## Приёмка

| Измеритель | Мы | Заказчик |
|------------|-----|----------|
| Карточка + кнопка + цвет | `[ ]` | `[ ]` |
| FIFO | `[ ]` | `[ ]` |
| Модификаторы | `[ ]` | `[ ]` |
| Витрина → табло → гость | `[ ]` | `[ ]` |

**Приёмка заказчика:** дата ______ · комментарий ______

### Артефакты

- `artifacts/demo-feedback/b21_stage0_mapping_2026-06-10.json`
- `artifacts/demo-feedback/screenshots/b21_barista_board_2026-06-10/`




Этап 1 — карточка (главная работа)
Файлы: _order_card.html.erb, стили в _layout.html.erb, preload в dashboard_controller.rb


 Квадратная карточка фиксированного размера

 Цвет по статусу: accepted синий · preparing оранжевый · ready зелёный

 Кнопка ≥80px на всю ширину: ГОТОВИТСЯ / ГОТОВ / Выдать

 Модификаторы: + КАПС жирным · БЕЗ … зачёркнуто

 Убрать мелкие кнопки «Принять →» / «✕»

 Починить N+1 по order_status_logs
Скрины по окончанию:

Скрин	Что показать
stage1_card_accepted.png
Колонка ACCEPTED — синяя карточка, большая кнопка
stage1_card_preparing.png
Колонка PREPARING — оранжевая
stage1_card_ready.png
Колонка READY — зелёная
stage1_modifiers.png
Крупный план: модификаторы + / БЕЗ
Артефакт: b21_stage1_card_ui_YYYY-MM-DD.json — какие файлы тронуты, PASS/FAIL по чеклисту этапа 1.

Этап 2 — FIFO `[x]` 2026-06-11
Файлы: `BoardOrdersQuery`, `OrderBoardBroadcaster`, `_orders_column`, `dashboard_controller`, `orders/update_status.turbo_stream.erb`

- [x] `created_at ASC` в колонке — `BoardOrdersQuery.for_column`
- [x] Убрать «Перетащите карточку…»
- [x] После смены статуса — resync колонки (FIFO в целевой колонке, не blind append)

**Не в этапе 2 (тащим дальше):** этап 3 WS/push гостю · этап 4 overlay отмены · этап 5 fly/smoke · цена/промокод убраны · `removed_modifiers` ключ готов · скрины localhost (Fly — этап 5)

Скрины по окончанию:

Скрин	Что показать
stage2_fifo_accepted.png
2+ заказа в accepted — старый сверху
stage2_after_status_move.png
После «ГОТОВИТСЯ» — карточка в preparing в правильном порядке
Артефакт: b21_stage2_fifo_YYYY-MM-DD.json — порядок created_at, нет drag-hint.

Этап 3 — гость (WS + push)
Файлы: OrderStatusUpdateService, GuestOrderBroadcaster, OrderStatusPushNotifier


 accepted → preparing: «Ваш заказ начали готовить»

 preparing → ready: «Заказ готов, забирайте!»

 WS на экране гостя ≤5 с

 Push (если FCM включён)
Скрины по окончанию:

Скрин	Что показать
stage3_guest_preparing.png
Экран гостя после «ГОТОВИТСЯ»
stage3_guest_ready.png
Экран гостя после «ГОТОВ»
stage3_push_optional.png
(опционально) push на телефоне
Артефакт: b21_stage3_guest_notify_YYYY-MM-DD.json — тексты, тайминг WS, push да/нет.

Этап 4 — отмена (упрощённо)
Файлы: _order_card.html.erb, cancel flow


 Overlay: тёмно-серый, «СТОП! ЗАКАЗ ОТМЕНЁН», кнопка подтверждения

 Без звука и списания
Скрины по окончанию:

Скрин	Что показать
stage4_cancel_overlay.png
Карточка с overlay отмены
stage4_cancel_confirmed.png
После подтверждения
Артефакт: b21_stage4_cancel_YYYY-MM-DD.json.

Этап 5 — финальная приёмка

 Тесты

 Smoke bin/b21_barista_board_fly_smoke.rb

 MCP Fly на coffeeos.fly.dev
Скрины по окончанию:

Скрин	Что показать
barista_board_after.png
Итоговое табло — все 3 колонки
stage5_e2e_vitrina_to_board.png
Заказ с витрины появился на табло
Артефакты:

Файл	Содержание
b21_acceptance_YYYY-MM-DD.json
Все критерии MVP §приёмка — PASS/FAIL
b21_mcp_fly_YYYY-MM-DD.json
MCP-прогон Fly

