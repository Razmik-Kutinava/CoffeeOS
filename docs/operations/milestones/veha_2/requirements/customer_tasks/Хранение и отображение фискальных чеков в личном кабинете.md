# Задача: Хранение и отображение фискальных чеков в личном кабинете

**CBR:** #73 · **Дата intake:** 2026-08-27 · **Редакция заказчика:** 2026-08-28  
**Артефакты:** `docs/operations/milestones/veha_2/artifacts/fiscal_receipts_personal_cabinet/`

---

Задача: Хранение и отображение фискальных чеков в личном кабинете
Бизнес-цель: обеспечить получение фискальных данных от кассы «Чеки Т-Бизнес», их надежное хранение и отображение пользователю в истории заказов PWA. Фискальный чек должен появляться в карточке заказа после фискализации оплаты, возврата или закрытия предоплаты, при этом повторная доставка уведомления от Т-Банка не должна создавать дубликаты.
1. Связь с картой интеграций
Затронутые сервисы из @INTEGRATIONS.md: Т-Банк Интернет-эквайринг / «Чеки Т-Бизнес».
Новый/изменяемый контракт: входящее HTTP(S)-уведомление о фискализации от Т-Банка.
Точка входа: NotificationURL, настроенный в терминале интернет-эквайринга (существующий эндпоинт `/callbacks/tbank`, различение типа уведомления — по полю Status).
Identity Mapping: внешний PaymentId / OrderId должен быть сопоставлен с внутренним платежом/заказом. Использовать существующий внутренний mapping, не создавать параллельную идентификацию заказа.
Фискальные данные: сохраняются только поля, фактически присутствующие в согласованной схеме уведомления: ссылка на чек, ФН/ФД/ФП и тип фискальной операции.
Raw payload: сохраняется исходный JSON уведомления для диагностики и возможности последующего разбора новых полей.
Обработка ошибок: невалидное уведомление не должно создавать фискальную запись; временная недоступность backend должна позволять корректно обработать повторную доставку после восстановления.
Ретраи: успешное уведомление должно получать HTTP 200 с телом OK; повторная доставка должна быть идемпотентной.
Смежные модули, которые НЕЛЬЗЯ ломать: существующая обработка платежных уведомлений, checkout/payment flow, история заказов, возвраты, авторизация PWA, существующий механизм сопоставления PaymentId/OrderId.
2. Разрешенный и Запрещенный Scope
Разрешено менять/создавать:
код-ревью и, при необходимости, доработка существующего обработчика TbankFiscalNotificationHandler и веток #72/#73;
исправление формата ответа платёжных (не-fiscal) веток в Callbacks::TbankController#notify (render json → render plain "OK");
backend-модуль обработки входящих платежных/фискальных нотификаций;
модели/миграции БД для хранения фискальных чеков;
сервис сопоставления платежа/заказа с фискальным уведомлением;
API истории/деталей заказов для передачи фискальных данных в PWA;
PWA-компоненты карточки и деталей заказа, отвечающие за отображение чеков;
конфигурацию NotificationURL и связанных integration settings;
integration/unit tests для фискализации;
INTEGRATIONS.md.
Строго запрещено менять:
существующую бизнес-логику оплаты без необходимости для обработки фискального уведомления;
существующую логику возврата платежа;
авторизацию и logout;
PLG-механику ЛК;
Telegram/email-каналы поддержки;
логику подписки и реферальной программы;
интеграцию с другими платежными провайдерами;
генерацию собственного QR по ФН/ФД/ФП;
реализацию несуществующего API запроса чека по PaymentId;
документацию Т-Банка как отдельную копию внешней документации.
3. Сценарии и Чек-лист (Gherkin)
Subtask 0: Аудит существующей реализации перед стартом работ
Given: диагностика бэкенда показала наличие обработчика TbankFiscalNotificationHandler, а также веток #72/#73 («Receipt + fiscal в ЛК»), находящихся в статусе REVIEW/push и ожидающих deploy-апрува.
When: команда приступает к реализации задачи.
Then: проведён код-ревью TbankFiscalNotificationHandler и содержимого веток #72/#73; список subtasks 1–30 сверен с уже реализованным кодом; каждый subtask, покрытый существующим кодом, помечен как «уже реализовано, требует code review и доработки» вместо «реализовать с нуля»; принято и задокументировано явное решение — дорабатывать существующие ветки или переписывать заново; точная схема фискального уведомления (Subtask 1) в первую очередь извлекается из кода TbankFiscalNotificationHandler, и только при недостаточности данных — запрашивается отдельно у Т-Банка.
Subtask 1: Получить точную схему уведомления о фискализации
Given: реализация зависит от фактического payload уведомления Т-Банка
When: команда получает документацию и тестовый пример именно фискального уведомления
Then: зафиксированы названия полей ссылки на чек, ФН/ФД/ФП, идентификатора операции и типа фискальной операции; без подтвержденной схемы production-обработчик не реализуется на предположительных полях.
Subtask 2: Зафиксировать mapping фискального уведомления на внутренний заказ
Given: согласованы поля PaymentId/OrderId внешнего уведомления
When: backend получает фискальное уведомление
Then: определяется существующий внутренний payment/order и фиксируется однозначное правило mapping.
Subtask 3: Настроить уведомления о фискализации на терминале
Given: доступен личный кабинет интернет-эквайринга Т-Банка
When: для нужного терминала включается отдельное уведомление о фискализации
Then: уведомление направляется на согласованный NotificationURL; настройки обычных платежных уведомлений не ломаются.
Subtask 4: Определить endpoint для фискальных уведомлений
Given: существующий backend принимает платежные нотификации на `/callbacks/tbank`, различая тип по полю Status.
When: определяется способ маршрутизации фискального payload.
Then: используется существующий endpoint `/callbacks/tbank` с надежным определением фискального типа уведомления по Status; решение зафиксировано в integration contract без создания дублирующего endpoint.
Subtask 5: Определить тип входящего уведомления
Given: backend получает входящий notification payload
When: payload проходит первичную обработку
Then: backend отличает фискальное уведомление от обычного платежного и направляет его только в соответствующий обработчик.
Subtask 6: Валидировать Token входящего уведомления
Given: backend получил фискальное уведомление
When: выполняется проверка подписи/Token по существующему безопасному механизму интеграции
Then: при невалидном Token payload отклоняется и фискальная запись не создается.
Subtask 7: Сохранить валидное фискальное уведомление
Given: уведомление прошло валидацию и заказ найден
When: обработчик сохраняет данные
Then: в БД сохраняются идентификатор заказа/платежа, ссылка на чек, доступные фискальные атрибуты, тип операции, время получения и raw JSON.
Subtask 8: Добавить идемпотентность обработки
Given: Т-Банк повторно отправляет то же уведомление
When: backend обрабатывает retry
Then: существующая запись определяется по согласованному уникальному идентификатору и новая запись-дубликат не создается.
Subtask 9: Вернуть успешный ответ Т-Банку
Given: валидное уведомление успешно обработано и сохранено
When: обработчик завершает обработку
Then: endpoint возвращает HTTP 200 и тело OK.
Subtask 9а: Исправить формат ответа для платёжных уведомлений
Given: Callbacks::TbankController#notify при обработке платёжных (не-fiscal) уведомлений возвращает JSON-ответ render json: { ok: true } (в том числе для сценариев успеха, дубликата и неизвестного статуса), тогда как fiscal-ветка (Status=RECEIPT) уже корректно возвращает render plain: "OK", status: :ok.
When: банк получает ответ на успешно обработанное платёжное уведомление.
Then: ответ приведён к формату render plain: "OK", status: :ok во всех успешных сценариях платёжной ветки (аналогично fiscal-ветке); ветка обработки ошибок (например, невалидный Token → 401) не изменяется; соответствующие тесты обновлены с проверки JSON-тела на проверку plain-text "OK"; docs/integrations/tbank.md обновлён с указанием причины изменения (соответствие официальному требованию Т-Банка, иначе банк ретраит уведомление по расписанию час → 24 часа → сутки → месяц).
Subtask 10: Обработать временную ошибку backend
Given: backend временно недоступен во время доставки уведомления
When: Т-Банк повторно доставляет уведомление после восстановления
Then: уведомление успешно обрабатывается, чек появляется в заказе и дубль не создается.
Subtask 11: Обработать уведомление для неизвестного заказа
Given: валидное уведомление содержит PaymentId/OrderId, для которого внутренний заказ не найден
When: backend пытается выполнить mapping
Then: уведомление не привязывается к произвольному заказу; событие логируется для диагностики согласно существующему monitoring/error flow.
Subtask 12: Создать модель хранения фискального чека
Given: определен окончательный payload фискального уведомления
When: создается структура хранения
Then: модель позволяет хранить несколько чеков для одного заказа и различать операции прихода, возврата и закрытия предоплаты.
Subtask 13: Добавить уникальное ограничение для идемпотентности
Given: один и тот же фискальный notification может прийти несколько раз
When: создается запись чека
Then: БД предотвращает создание дубликата по согласованному внешнему идентификатору/комбинации идентификаторов.
Subtask 14: Передать фискальные данные через API истории заказов
Given: у заказа существует сохраненный фискальный чек
When: PWA запрашивает историю/детали заказа
Then: API возвращает необходимые для UI данные чека без передачи внутренних служебных данных, если они не нужны клиенту.
Subtask 15: Отобразить секцию «Чек» в карточке заказа
Given: API вернул фискальный чек
When: пользователь открывает заказ в ЛК
Then: отображается секция «Чек» со ссылкой или другим согласованным способом доступа к готовому чеку.
Subtask 16: Реализовать переход по ссылке готового чека
Given: касса вернула валидную ссылку на внешний чек
When: пользователь нажимает на чек
Then: открывается внешний документ чека способом, согласованным для PWA; собственный QR по ФН/ФД/ФП не генерируется.
Subtask 17: Обработать состояние «Чек формируется»
Given: платеж подтвержден, но фискальное уведомление еще не поступило
When: пользователь открывает заказ
Then: отображается состояние «Чек формируется» без ошибки и пустого broken-state.
Subtask 18: Обновить состояние после поступления чека
Given: ранее заказ отображался со статусом «Чек формируется»
When: фискальное уведомление сохранено
Then: при следующем получении данных заказа пользователь видит доступный чек.
Subtask 19: Отобразить несколько чеков одного заказа
Given: для одного заказа существуют несколько фискальных операций
When: API возвращает несколько чеков
Then: PWA отображает их отдельно и не перезаписывает предыдущий чек.
Subtask 20: Отобразить чек возврата
Given: по заказу выполнен частичный или полный возврат и получено соответствующее фискальное уведомление
When: пользователь открывает детали заказа
Then: чек возврата отображается отдельно с корректным типом операции.
Subtask 21: Отобразить закрывающий чек предоплаты
Given: заказ с предоплатой был закрыт и касса отправила фискальное уведомление
When: пользователь открывает историю заказа
Then: закрывающий чек отображается как отдельная фискальная операция.
Subtask 22: Обработать отсутствие фискального уведомления
Given: ожидаемое уведомление не поступило в разумный срок
When: пользователь открывает заказ
Then: отображается состояние формирования чека, а отсутствие чека не приводит к ошибке экрана заказа.
Subtask 23: Зафиксировать процесс восстановления пропущенного уведомления
Given: уведомление не было получено или истекло стандартное окно доставки
When: оператор диагностирует отсутствие чека
Then: документирован ручной процесс повторного получения/эскалации через архив уведомлений Т-Банка в пределах доступного срока хранения.
Subtask 24: Покрыть успешную оплату интеграционным тестом
Given: существует оплаченный заказ
When: backend получает валидное фискальное уведомление с данными чека
Then: чек сохраняется и становится доступен через API истории/деталей заказа.
Subtask 25: Покрыть возврат интеграционным тестом
Given: существует оплаченный заказ с частичным возвратом
When: приходит фискальное уведомление возврата
Then: создается отдельная запись чека возврата, не заменяющая чек первоначальной оплаты.
Subtask 26: Покрыть закрытие предоплаты интеграционным тестом
Given: существует заказ с предоплатой
When: приходит уведомление о закрывающей фискальной операции
Then: закрывающий чек сохраняется и отображается отдельно.
Subtask 27: Покрыть retry-сценарий
Given: одно и то же уведомление доставлено дважды
When: оба payload проходят обработку
Then: в БД существует только одна запись соответствующего чека.
Subtask 28: Покрыть задержку фискализации
Given: платеж подтвержден, но фискальное уведомление отсутствует
When: PWA получает детали заказа
Then: UI показывает «Чек формируется», а после появления уведомления — ссылку на чек.
Subtask 29: Проверить регрессию существующих платежных уведомлений
Given: backend уже обрабатывает обычные платежные notification
When: добавляется обработка фискальных notification
Then: существующая обработка платежных статусов продолжает работать без изменения бизнес-семантики.
Subtask 30: Проверить границы scope
Given: функциональность фискальных чеков завершена
When: выполняется ревью изменений
Then: отсутствуют собственная генерация QR, попытки обращения к несуществующему API кассы для получения чека и изменения сторонних платежных интеграций без необходимости.
4. Команды TDD-проверки
Запуск интеграционных тестов: npm test tests/integration/fiscal-receipts.test.ts
Проверка типов: npx tsc --noEmit
Проверка миграций/схемы БД: команда, используемая текущим репозиторием для проверки миграций.
Дополнительно: проверить реальную доставку фискального notification на staging/test terminal после включения соответствующей настройки Т-Банка.
5. Блокирующие зависимости
До реализации обработчика необходимо получить и зафиксировать (в первую очередь — из кода TbankFiscalNotificationHandler и веток #72/#73, затем — при недостаточности данных — у Т-Банка):
точную схему фискального notification;
пример реального payload;
поле/поля со ссылкой на чек;
наличие и формат ФН/ФД/ФП;
способ определения типа фискальной операции;
внешний идентификатор, пригодный для идемпотентности;
подтверждение, что используется существующий endpoint `/callbacks/tbank`;
согласованный mapping PaymentId/OrderId на внутренний заказ;
способ отображения внешней страницы чека в PWA: новая вкладка, iframe или другой UX-вариант.
Без этих данных запрещено реализовывать обработчик на основе предположительной структуры payload.
TODO:
Subtask 0: Аудит существующей реализации перед стартом работ
Subtask 1: Получить точную схему уведомления о фискализации
Subtask 2: Зафиксировать mapping фискального уведомления на внутренний заказ
Subtask 3: Настроить уведомления о фискализации на терминале
Subtask 4: Определить endpoint для фискальных уведомлений
Subtask 5: Определить тип входящего уведомления
Subtask 6: Валидировать Token входящего уведомления
Subtask 7: Сохранить валидное фискальное уведомление
Subtask 8: Добавить идемпотентность обработки
Subtask 9: Вернуть успешный ответ Т-Банку
Subtask 9а: Исправить формат ответа для платёжных уведомлений
Subtask 10: Обработать временную ошибку backend
Subtask 11: Обработать уведомление для неизвестного заказа
Subtask 12: Создать модель хранения фискального чека
Subtask 13: Добавить уникальное ограничение для идемпотентности
Subtask 14: Передать фискальные данные через API истории заказов
Subtask 15: Отобразить секцию «Чек» в карточке заказа
Subtask 16: Реализовать переход по ссылке готового чека
Subtask 17: Обработать состояние «Чек формируется»
Subtask 18: Обновить состояние после поступления чека
Subtask 19: Отобразить несколько чеков одного заказа
Subtask 20: Отобразить чек возврата
Subtask 21: Отобразить закрывающий чек предоплаты
Subtask 22: Обработать отсутствие фискального уведомления
Subtask 23: Зафиксировать процесс восстановления пропущенного уведомления
Subtask 24: Покрыть успешную оплату интеграционным тестом
Subtask 25: Покрыть возврат интеграционным тестом
Subtask 26: Покрыть закрытие предоплаты интеграционным тестом
Subtask 27: Покрыть retry-сценарий
Subtask 28: Покрыть задержку фискализации
Subtask 29: Проверить регрессию существующих платежных уведомлений
Subtask 30: Проверить границы scope
Изменения в связке: Т-Банк Интернет-эквайринг — фискализация
Точки входа: NotificationURL backend-приложения — существующий эндпоинт `/callbacks/tbank`, обрабатывающий отдельный тип уведомлений о фискализации по полю Status.
Identity Mapping: внешний PaymentId/OrderId сопоставляется с существующим внутренним payment/order. Фискальная запись не создается без успешного mapping на внутренний заказ.
Handling Errors: валидное уведомление после успешного сохранения получает HTTP 200 и тело OK (включая исправленные платёжные ветки, см. Subtask 9а). Повторная доставка должна быть идемпотентной. При временной недоступности backend обработка выполняется при последующем retry Т-Банка. Уведомление с неизвестным заказом не привязывается к другому заказу и отправляется в существующий механизм диагностики.
Security: входящий Token обязательно валидируется до записи данных. Секреты/ключи интеграции хранятся в .env/существующем secret storage и не передаются в PWA.
Stored Data: сохраняются только подтвержденные поля фискального payload: ссылка на чек, доступные ФН/ФД/ФП, тип операции, время получения, внешний идентификатор и raw JSON.
Idempotency: повторное уведомление не должно создавать новую запись. Для этого используется уникальный внешний идентификатор или согласованная комбинация полей.
PWA Contract: API истории/деталей заказа возвращает пользователю только необходимые для отображения данные фискального чека.
Recovery: при отсутствии notification предусматривается ручная процедура восстановления через доступный архив уведомлений Т-Банка; детали процедуры фиксируются после подтверждения актуального процесса со стороны Т-Банка.
Out of scope: запрос фискального чека через API кассы по PaymentId, генерация собственного QR по ФН/ФД/ФП, копирование документации Т-Банка в проект.

---

## Заметки агента

- **Серия после #72:** в заметках #72 это следующая задача security/product серии (хранение/отображение чеков в ЛК).
- **Блокер до GREEN:** Subtask 1 — точная схема фискального notification + пример payload. Без неё production-обработчик на предположительных полях **запрещён** ТЗ.
- **Канон тестов CoffeeOS:** Minitest `bin/rails test …` + при необходимости `node --test test/javascript/*.mjs` (не `npm test` / `tsc` из формулировки ТЗ, если в репо нет такого контура).
- **Не путать с #72:** #72 = исходящий Receipt.Email/Phone в Init; #73 = входящие уведомления о фискализации + хранение + UI в ЛК.
- **Subtask 3 (терминал Т-Банка):** настройка NotificationURL в кабинете — ops владельца / апрув, не код агента alone.
