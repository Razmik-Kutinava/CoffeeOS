# После #42 пропали повторы в шторке и история покупок

**Дата:** 2026-08-06  
**Источник:** Арам · Telegram / чат  
**Артефакты:** `docs/operations/milestones/veha_2/artifacts/repeat_hidden_by_stale_active_orders/`

---

## Текст заказчика (1:1)

Это о'кей, но обрати внимание:

- нет повторов в штоке
- нет кнопки в карточке товара

Мой профиль, то есть не подгрузил историю покупок

то есть не подгрузилось еще

---

## Скрины из сообщения

| Файл | Содержание |
|------|------------|
| `screenshots/01_telegram_aram_no_repeats_profile.png` | Коллаж: каталог Point A (шторка без «повторить») · карточка «Фильтр-кофе Бразилия» · профиль Aram R (email/phone подтверждены) |

---

## Заметки агента

**Связь:** follow-up после Fly **v433** (#42 TTL на `orders/active`).

**Root cause (диагностика):** `Shop::CustomerFrequentProductsService#has_active_order?` без TTL — June `accepted` (`#202606-*`) → `has_active_order: true` → `frequent_items: []` → нет секции «повторить» и кнопок «оплатить в 1 клик» на карточках повтора. Профиль контакты грузятся (сессия ок); «история покупок» = частоты/повторы.

**План:** выровнять `has_active_order?` с окном 24h (#42); bust cache; тесты; deploy/MCP.
