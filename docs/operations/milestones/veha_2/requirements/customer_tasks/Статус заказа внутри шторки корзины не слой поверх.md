# Статус заказа внутри шторки корзины (не слой поверх)

**Дата интейка:** 2026-07-31  
**Источник:** фидбек заказчика в чате (+ скрин)  
**Артефакты:** `docs/operations/milestones/veha_2/artifacts/status_inside_cart_sheet/`

---

## Текст заказчика (1:1)

Смотри уже лучше, но это нужно вписать в шторку, а не поверх её ебашь, слой сверху.

---

## Заметки агента

- Скрин из чата: статус-блок + «Потеряно соединение…» визуально лежит **поверх** cart bottom sheet (отдельный fixed/z-index), а не внутри её DOM/скролла.
- Канон: `OrderStatusSheet` → **секция внутри** `CartSheet` (relative flow), убрать sibling-mount из `App.svelte` и `position: fixed; z-index: 60` как отдельный overlay.
- Связь: #35 compact status, #36 accordion, Quick Repeat hide — не ломать; меняется только слой/mount.
