# Bottom Sheet: Expanded Mode Layout Fix & Internal Grid Refactor

> **Статус:** **RESTART** 2026-07-21 · код `7683dee` **откачен** (сетка была применена к шторке по букве ТЗ — владелец уточнил: речь о каталоге) · новое SPEC **[ ]**  
> **Дата ТЗ:** 2026-07-21  
> **Источник:** заказчик (чат)  
> **Артефакты / скрины:** [`../../artifacts/bottom_sheet_expanded_grid/`](../../artifacts/bottom_sheet_expanded_grid/README.md)

---

## Кратко (для агента)

| Поле | Содержание |
|------|------------|
| Проблема | В expanded виден второй ряд каталога; внутри шторки нет сетки 4 в ряд и внутреннего скролла |
| Цель | Expanded до первого ряда каталога + сетка товаров 4 в ряд с вертикальным скроллом при 5+ |
| Не трогать | State корзины, кнопки +/−/Удалить, header шторки, кнопку суммы «+…₽» |
| Код | **запрещён** до `go` после PHASE 1: SPEC |

---

## Текст заказчика (канон — без сокращений)

# Bottom Sheet: Expanded Mode Layout Fix & Internal Grid Refactor

## Бизнес-контекст
В режиме expanded bottom sheet перекрывает каталог некорректно — виден второй ряд товаров, что нарушает визуальную иерархию и UX. Внутри шторки товары отображаются в неоптимальном формате: нет фиксированной сетки 4 в ряд, отсутствует внутренний скролл при переполнении. Необходимо привести layout к UX-паттернам, зафиксированным в cursor rules.

## Глобальные ограничения
- Не менять структуру данных товаров и корзину (state management).
- Не трогать логику добавления/удаления товаров (кнопки +/−/Удалить).
- Не менять header шторки (адрес, профиль, время работы).
- Не менять кнопку "+184₽" (итоговая сумма).
- Размеры карточек и отступы брать строго из cursor rules (UX паттерны).

## Сценарии реализации (Use Cases в Gherkin)

### Шаг 1 — Expanded mode: высота шторки до первого ряда каталога

- [ ] Given: Bottom sheet в collapsed состоянии, каталог отображает категории "Черный" и "Фильр-кофе" с двумя рядами товаров в каждой
- When: Пользователь тянет шторку вверх (swipe up) или тапает на handle
- Then: Шторка переходит в expanded mode, её верхний край совпадает с началом второго ряда товаров категории "Фильр-кофе" (виден только первый ряд каждой категории каталога)

- [ ] Given: Bottom sheet в expanded mode
- When: Пользователь тянет шторку вниз (swipe down)
- Then: Шторка возвращается в collapsed mode, каталог полностью видим

### Шаг 2 — Внутренняя сетка товаров: 4 в ряд

- [ ] Given: Bottom sheet в expanded mode, в списке 4 товара
- When: Шторка отрисована
- Then: Товары отображаются в сетке 4 в ряд (горизонтально), карточки имеют размеры согласно cursor rules UX паттернам

- [ ] Given: Bottom sheet в expanded mode, в списке 5+ товаров
- When: Шторка отрисована
- Then: Первые 4 товара видны в первом ряду, остальные доступны через вертикальный скролл внутри области товаров

- [ ] Given: Bottom sheet в expanded mode, в списке 1–3 товара
- When: Шторка отрисована
- Then: Товары отображаются в одном ряду (1–3 карточки), скролл не активируется

### Шаг 3 — Размеры карточек и отступы

- [ ] Given: Bottom sheet в expanded mode
- When: Отрисовка карточек товаров
- Then: Ширина карточки, высота, padding, gap между карточками соответствуют значениям из cursor rules (UX паттерны для mobile grid)

## Экстремальные сценарии (Error & Edge States)

- **1 товар в списке**: карточка занимает 1/4 ширины ряда (не растягивается на всю ширину), сетка сохраняется.
- **0 товаров (пустая корзина)**: отображается пустое состояние "Корзина пуста", шторка не блокирует interaction.
- **Длинное название товара**: текст обрезается с ellipsis после N строк (согласно cursor rules), не ломает высоту карточки.
- **Нет фото товара**: placeholder "Нет фото" отображается в том же размере карточки, без деформации сетки.
- **Быстрый swipe up/down**: анимация не прерывается, высота корректно фиксируется на snap point.
- **Ресайз окна (tablet/desktop)**: сетка пересчитывается, но логика "4 в ряд + скролл" сохраняется.

## Критерии авто-выхода (Exit Criteria)

1. Код проходит unit/интеграционные тесты под эти сценарии.
2. Проверка типов TypeScript (tsc) и линтер проходят без ошибок сборки.
3. Визуальный тест: в expanded mode виден ровно 1 ряд товаров каталога (второй ряд полностью скрыт под шторкой).
4. Визуальный тест: внутри шторки при 4+ товарах активируется вертикальный скролл, при ≤4 — не активируется.

---

## Заметки агента

- Затронутые файлы (разведка до SPEC): `app/frontend/components/CartSheet.svelte` (ветка `MODE_EXPANDED && count >= 2`), `app/frontend/lib/cartSheetThresholds.js` (`SHEET_VH.expandedMulti`).
- По скрину заказчика 2026-07-21 высота expanded уже близка к цели (виден 1 ряд каталога) — Шаг 1 скорее верификация тестом, основная работа — Шаг 2–3 (сетка).
- Открытые вопросы к владельцу (до SPEC): источник размеров карточек (в cursor rules числовых UX-паттернов сетки нет — предлагается канон из peek-карточек `w-[min(30vw,118px)]`); поведение на tablet/desktop при `max-width: 414px` шторки; фронт на JS — вместо `tsc` линт/сборка Vite.

### RESTART 2026-07-21 (уточнение владельца)

- Первая реализация (RED `273a43c` → GREEN `7683dee`) применила сетку 4 в ряд **внутри шторки expanded** — по букве ТЗ («Внутри шторки товары отображаются в неоптимальном формате: нет фиксированной сетки 4 в ряд»).
- **Уточнение владельца:** «сетка 4 в ряд» относится к **каталогу товаров на главной**, а не к содержимому корзины в шторке; expanded-корзина остаётся горизонтальными строками списком (канон S2).
- Код откачен к состоянию до RED (регрессия cart sheet 56/0 после отката). Задача перезапускается: новое SPEC после уточнённого ТЗ / решений владельца по каталогу (карточки при 4-в-ряд на 414px ≈ 93px — сильно мельче текущих 8.5rem).
