# Эпик: Рекуррентные платежи и UX оплаты витрины (Т-Банк)

**ID:** B1.12 · **Источник:** заказчик, документы эпика (3 задачи)

### Канон (читать только это)

| | |
|--|--|
| **ТЗ** | **rev2** (2026-06-24) — nonPCI + кастомный UI + FSM 0–7. Тексты **v1** (iframe, «всегда без галочки») — **архив**, не ТЗ. |
| **Код** | R1 + R2 + R3 **`[x]`** OPS_PASS · Fly MCP **10/10** · RSA Fly **`[x]`** |
| **Q-R2-1..3** | **`[x]`** закрыты 2026-06-24 (nonPCI, тумблер default on, макеты 8924/8925) |
| **Открыто** | **Приёмка:** после 1-й оплаты с тумблером «Использовать карту…» карта **не появляется** на 2-м заказе (стенд заказчика). Эпик **не закрыт** до фикса + апрува. |
| **Апрув** | `[ ]` |

**Имена (канон ops / код / SQL):**

| В тексте заказчика | У нас |
|--------------------|--------|
| таблица **UserCards** | **`mobile_payment_methods`** |
| **user_id** | **`customer_id`** → `MobileCustomer` (гость витрины, не `users`) |
| **rebill_id** | **`card_token`** |
| **card_id** (банка) | **`bank_card_id`** |
| **pan** (маска) | **`card_masked`** / `pan_display` |

Дальше в ops и коде — **только наши имена**.

**Подзадачи (история):** R1 бэкенд · R2 форма карты · R3 FSM — **все `[x]`**.  
**Связано:** [B1.7](B1_7_checkout_order_screen.md) · §2.3 · `Payments::TbankAdapter` · `POST /callbacks/tbank`  
**Стенд:** `https://coffeeos.fly.dev/shop?tenant_id=655aaccb-004a-4bb9-a50a-ce618854dda3` · `#/checkout`  
**Runbook:** [`TBANK_RECURRENT.md`](../../runbooks/TBANK_RECURRENT.md)

**Артефакты rev2 (канон):**
- [`b112_revision2_stage0_scope_2026-06-24.json`](../../artifacts/demo-feedback/b112_revision2_stage0_scope_2026-06-24.json)
- [`b112_tbank_nonpci_review_2026-06-24.json`](../../artifacts/demo-feedback/b112_tbank_nonpci_review_2026-06-24.json)
- Макеты: [`1000008925.png`](../../artifacts/demo-feedback/screenshots/1000008925.png) (R2) · [`1000008924.png`](../../artifacts/demo-feedback/screenshots/1000008924.png) (R3)

**Архив v1 (не читать как ТЗ):** [`b112_stage0_scope_2026-06-18.json`](../../artifacts/demo-feedback/b112_stage0_scope_2026-06-18.json) · строки DEMO_FEEDBACK до 2026-06-24 с iframe / «без галочки».

**Баг приёмки (открыт):** полный текст заказчика + чеклист работ — **§ «Баг приёмки B1.12-BUG-SAVE»** ниже.  
**Скрин:** [`b112_save_card_toggle_on_customer_2026-07-04.png`](../../artifacts/demo-feedback/screenshots/b112_save_card_toggle_on_customer_2026-07-04.png)  
**JSON:** [`b112_bug_save_card_customer_2026-07-04.json`](../../artifacts/demo-feedback/b112_bug_save_card_customer_2026-07-04.json)

---

## Баг приёмки B1.12-BUG-SAVE (текст заказчика — дословно)

> Зафиксировано 2026-07-04 · статус **open** · приоритет **высокий**  
> В коде/SQL смотреть **`mobile_payment_methods`** / **`customer_id`** (в тексте заказчика: UserCards / user_id — см. таблицу имён в шапке).

### Баг-репорт: резюме из задач по оплате с банком

**Не сохраняется карта в БД при первой оплате с включенным тумблером «Использовать карту для будущих заказов»**

**Приоритет:**  
Высокий (блокирует ключевую бизнес-цель рекуррентных платежей и оплаты в 1 клик)

**Описание:**

**Шаги к воспроизведению:**

1. Открыть экран оформления заказа.
2. Выбрать способ оплаты «Новая карта».
3. Ввести валидные тестовые данные карты (PAN, срок действия, CVC).
4. Убедиться, что тумблер «Использовать карту для будущих заказов» находится в активном состоянии (включен).
5. Нажать кнопку «Оплатить», успешно пройти сценарий оплаты (включая 3DS, если триггерится).
6. Дождаться перехода на экран «Спасибо за покупку» (статус CONFIRMED).
7. Оформить новый заказ и перейти к выбору способа оплаты.

**Фактический результат:**  
В списке способов оплаты новая карта отсутствует. Система снова предлагает ввести данные карты с нуля. Запись в таблице UserCards для данного пользователя не создается (или создается без привязки к user_id).

**Ожидаемый результат:**  
После успешной оплаты с активным тумблером «Использовать карту для будущих заказов» карта должна быть токенизирована и сохранена в БД. При оформлении следующего заказа эта карта должна отображаться в списке сохраненных методов оплаты для выбора.

**Требование (ссылка на задачу):**  
Задача: «Архитектура СУБД и интеграция API Т-Банка (Рекуррентные платежи)»  
Пункт 2.a.iii: «При успехе и save_card == true извлекает RebillId, CardId и маску карты, сохраняя их в таблицу UserCards».  
Критерий приёмки №2: «При успешной эмуляции вебхука от Т-Банка RebillId гарантированно записывается в БД».

**Дополнительно:**  
Скриншот: Приложен (экран ввода карты, тумблер «Использовать карту для будущих заказов» активен) — [`b112_save_card_toggle_on_customer_2026-07-04.png`](../../artifacts/demo-feedback/screenshots/b112_save_card_toggle_on_customer_2026-07-04.png).  
Логи/Доп. файлы: Для ускорения фикса приложить JSON запроса к эндпоинту инициализации оплаты (проверить наличие флага `save_card: true` и корректность зашифрованного CardData), а также логи ответа от Т-Банка (наличие RebillId в успешном вебхуке).

### Фиксация (Workflow) — текст заказчика

**БЫЛО — СТАЛО:**

**БЫЛО:** Проблема сформирована в свободной форме, не привязана к конкретной задаче и не соответствует DOR бага (отсутствовали шаги, ссылка на требование, четкое разделение факта и ожидания).

**СТАЛО:** Проблема классифицирована, определена целевая задача (Бэкенд: Архитектура СУБД и интеграция API Т-Банка). Составлен структурированный баг-репорт по шаблону DOR бага с указанием шагов, факта, ожидания и прямой ссылки на критерии приёмки. Из текста удалены стоп-слова и неопределенные формулировки.

**БЭКЛОГ (осталось на ручной режим / требует уточнения):**

- Проверить на стороне фронтенда (задача «Фронтенд. Кастомный ввод карты...»), корректно ли передается флаг `save_card` в теле запроса при нажатии на кнопку «Оплатить».
- Проверить сетевые запросы (вкладка Network), чтобы убедиться, что открытый PAN/CVV не уходят на бэкенд (соответствие критерию №2 фронтенд-задачи).

### Чеклист работ по багу (агент — по порядку)

- [x] **D1** — Сверить deploy Fly: на стенде тот же коммит, что с B1.12 rev2 nonPCI / `save_card` · **PASS** 2026-07-04 — Fly v327 (2026-07-02) ≈ `origin/develop` `2a34ada` (R1–R3 в истории); `card_config` → 401 не 404 · [`b112_bug_save_card_d1_deploy_2026-07-04.json`](../../artifacts/demo-feedback/b112_bug_save_card_d1_deploy_2026-07-04.json) · **баг не из-за отсутствия деплоя фичи**
- [ ] **D2** — Один прогон оплаты: Network `POST …/payments/new_card` (или актуальный эндпоинт) — есть ли `save_card: true` и зашифрованный `card_data` (без открытого PAN/CVV)
- [ ] **D3** — Ответ Т-Банка / наш API после оплаты: есть ли **RebillId** при CONFIRMED (FinishAuthorize, webhook, GetState/finalize)
- [ ] **D4** — SQL: строка в `mobile_payment_methods` для `customer_id` заказа; если нет — где оборвался `SavedCardStore`
- [ ] **D5** — Второй заказ: `GET /shop/api/saved_cards` — тот же email / сессия; есть ли `primary` в JSON
- [ ] **F1** — Фикс по результату D1–D5 (только нужный слой: банк / бэкенд / сессия UI)
- [ ] **T1** — Тест на изменение + регрессия shop payment / saved_cards
- [ ] **A1** — Артефакт post-fix + апрув заказчика · закрыть баг в ISSUES

**Правило ветвления:** нет RebillId → интеграция/терминал; RebillId есть, строки в БД нет → бэкенд; строка есть, UI пустой → сессия/`saved_cards`.

---

## Что в коде сейчас (rev2 — канон)

| Тема | Реализация |
|------|------------|
| **Первая оплата** | nonPCI: Init → FinishAuthorize + `CardData` (RSA на клиенте) |
| **Повторная** | Init → Charge по RebillId · silent · FSM в кнопке |
| **Сохранённые карты** | `mobile_payment_methods` (`card_token`, `card_masked`, `bank_card_id`) |
| **Ввод карты** | `NewCardSheet.svelte` по макету 8925 · без iframe виджета банка |
| **Кнопка оплаты** | FSM 0–7 · макет 8924 |
| **3DS** | `3DS_CHECKING` → ACSUrl + overlay iframe |
| **Ошибки** | ErrorCode Т-Банка на фронт без искажения |
| **Сохранение карты** | Тумблер «Использовать карту…» · `save_card` · **default on** |
| **Карты на гостя** | Все храним · главная = последняя успешная (Q2) |
| **СБП / наличные** | Card recurrent only (Q3) |

### Карта кода

| Зона | R | Файлы |
|------|---|--------|
| БД | R1 | `mobile_payment_methods` — `bank_card_id`, RebillId → `card_token` |
| Адаптер | R1 | `tbank_adapter.rb` — `finish_authorize`, 3DS |
| Shop API | R1–R3 | new card + charge by `saved_card_id` · `GET /saved_cards` |
| Фронт форма | R2 | `NewCardSheet.svelte` · `tbankCardEncrypt.js` |
| Фронт checkout | R3 | `PaymentMethodsSheet.svelte` · `shopPayFsm.js` |
| Сохранение | R1 | `Payments::SavedCardStore` · webhook / GetState / FinishAuthorize |

---

## Прогресс

### Ревизия 2 — **код закрыт**, открыта приёмка

```
[x] 0…0d — docs + Q-R2-1..3
[x] R1 — nonPCI бэкенд (`b112_r1_nonpci_ops_pass_2026-06-24.json`)
[x] R2 — кастомная форма (`b112_r2_custom_card_ops_pass_2026-06-24.json`)
[x] R3 — FSM + Fly MCP 10/10 (`b112_r3_fsm_ops_pass_2026-06-25.json`) · RSA Fly
[ ] Приёмка: карта сохраняется после 1-й оплаты (тумблер on) → видна на 2-м заказе
[ ] Апрув заказчика на эпик B1.12 rev2
```

**Следующий шаг агента:** диагностика/фикс сохранения карты на стенде (не переписывать R1–R3 с нуля).

### Legacy v1 (архив, 2026-06-18…21)

Реализовано и заменено rev2. Старые баги 2-й оплаты (iframe / CVC) чинились в v1/R4–R6; **текущий открытый баг приёмки** — см. блок «Открыто» в шапке (не «scope v1 ≠ v2»).

---

## Чеклист gate (docs rev2) — **закрыт**

- [x] ТЗ v2 — дословный текст заказчика (3 задачи) в этом файле
- [x] Scope rev2 + имена `mobile_payment_methods`
- [x] Конфликты Q1/Q5/Q6 → Q-R2-1..3 **закрыты**
- [x] JSON / сверка Т-Банк / макеты / ops-индексы
- [x] Q-R2-1..3 · `go` R1 · R2 · R3 — **выполнены**

---

## Текст заказчика (дословно) — ревизия 2026-06-24

### Задача 1 — B1.12-R1: Архитектура СУБД и интеграция API Т-Банка (Рекуррентные платежи)

**Приоритет:** Высокий  
**Компонент:** Бэкенд / СУБД

**Описание**

Для реализации бесшовной оплаты в один клик без перехода на внешние платежные шлюзы необходимо настроить бэкенд-интеграцию с эквайрингом Т-Банка (сценарий nonPCI). Система должна поддерживать токенизацию карт при первой оплате и проведение рекуррентных платежей (списание по токену) без участия пользователя.

**Технические требования**

**СУБД (Таблица UserCards):**

Создать или обновить структуру таблицы для хранения привязанных карт:

- `id` (UUID) — внутренний ID карты.
- `user_id` (INT/UUID) — внешний ключ пользователя.
- `rebill_id` (STRING) — токен рекуррентного платежа от Т-Банка.
- `card_id` (STRING) — ID карты в системе банка.
- `pan` (STRING) — маскированный номер для UI (формат: *5953, как на макете 1000008924.png).
- `exp_date` (STRING) — срок действия (ММ/ГГ).
- `card_type` (STRING) — платежная система (MIR, VISA, MASTERCARD) для отображения иконок.

Ограничение безопасности: Хранение полных данных (PAN, CVC/CVV) категорически запрещено.

**Логика API-методов:**

**Эндпоинт 1: Инициализация оплаты новой карты.** Принимает зашифрованный на фронте CardData, amount и флаг save_card.

- Вызывает метод Т-Банка /Init с уникальным CustomerKey. Получает PaymentId.
- Вызывает метод Т-Банка /FinishAuthorize, передавая PaymentId и CardData.
- При успехе и save_card == true извлекает RebillId, CardId и маску карты, сохраняя их в таблицу UserCards.

**Эндпоинт 2: Оплата в 1 клик (По токену).** Принимает card_id (из нашей БД) и amount.

- Достает из базы соответствующий RebillId.
- Вызывает метод Т-Банка /Init (получает PaymentId).
- Вызывает метод Т-Банка /Charge, передавая PaymentId и RebillId. Транзакция проходит скрыто (silent request).

**Обработка 3D-Secure:** Если банк на любом этапе возвращает статус 3DS_CHECKING, бэкенд должен транслировать фронтенду параметры ACSUrl, PaReq и MD для прохождения аутентификации.

**Критерии приёмки (Acceptance Criteria)**

- API полностью покрывает сценарии первой оплаты и рекуррентного списания.
- При успешной эмуляции вебхука от Т-Банка RebillId гарантированно записывается в БД.
- Ошибки API Т-Банка логируются и пробрасываются на фронтенд в неизменном виде (с сохранением исходных кодов ошибок ErrorCode).

---

### Задача 2 — B1.12-R2: Фронтенд. Кастомный ввод карты и клиентское RSA-шифрование

**Приоритет:** Высокий  
**Компонент:** Фронтенд (PWA / Mobile First)

**Описание**

Необходимо сверстать полностью кастомный интерфейс ввода новой карты внутри приложения, полностью отказавшись от стандартных iframe-виджетов банка, которые ломают темную тему и дизайн-систему. Передача данных карты должна быть безопасной и соответствовать требованиям сценария nonPCI Т-Банка.

**Технические требования**

**Верстка интерфейса:**

- Сверстать шторку/экран «Новая карта» строго по макету 1000008925.png.
- Поля: Номер карты, ММ/ГГ, CVC/CVV.
- Кастомный тумблер: «Использовать карту для будущих заказов» (по умолчанию активен).
- Блок с логотипами систем безопасности внизу экрана.

**Валидация и маски:**

- Навесить маску на ввод номера карты (разделение по 4 цифры) и срок действия (автоматический слэш /).
- Реализовать валидацию номера карты по алгоритму Луна (Luhn algorithm) на лету. При невалидном номере или пустых полях кнопка «Оплатить» блокируется, инпуты подсвечиваются ошибкой.

**Безопасность и шифрование (Критично):**

- Запрещено отправлять данные карты на наш бэкенд в открытом виде.
- Реализовать сбор данных из инпутов в строку формата: PAN;ExpDate;CVV.
- Реализовать шифрование этой строки на стороне клиента алгоритмом RSA-2048 с использованием публичного ключа нашего терминала Т-Банка (в соответствии с документацией Т-Банка по ручной сборке параметра CardData).
- На бэкенд отправляется исключительно готовая зашифрованная строка CardData.

**Критерии приёмки (Acceptance Criteria)**

- Визуальное соответствие макету 1000008925.png — 100% (цвета, шрифты, скругления, поведение тумблера).
- Вкладка Network в DevTools подтверждает: открытый PAN и CVV не фигурируют ни в одном сетевом запросе.
- Валидация полей отрабатывает до отправки запроса на сервер.

**Макет:** [`1000008925.png`](../../artifacts/demo-feedback/screenshots/1000008925.png)

---

### Задача 3 — B1.12-R3: Фронтенд. Реализация стейт-машины динамической кнопки оплаты

**Приоритет:** Блокирующий  
**Компонент:** Фронтенд (PWA / Mobile First)

**Описание**

Для максимизации конверсии повторных покупок необходимо избавиться от промежуточных экранов ошибок и модальных окон. Кнопка «Оплатить» на экране выбора способов оплаты (1000008924.png) должна работать как конечный автомат (State Machine), транслируя текущий статус соединения и ответы банка прямо внутри своего интерфейса.

**Технические требования**

Реализовать стейт-машину для кнопки «Оплатить». Текст, цвет и доступность кнопки должны меняться в зависимости от этапа выполнения запроса и привязываться к ErrorCode от бэкенда/Т-Банка:

| Состояние (State) | Событие (Триггер) | Визуальное состояние кнопки | Действие системы / Логика |
|-------------------|-------------------|----------------------------|---------------------------|
| 0. Default | Экран загружен, карта выбрана | Синяя кнопка, текст: Оплатить. Кликабельна. | Ожидание тапа пользователя. |
| 1. Connecting | Пользователь нажал кнопку | Текст: Установка соединения... + лоадер. Кнопка disabled. | Отправка запроса инициализации на наш бэкенд. |
| 2. Processing | Бэк вернул успешный /Init | Текст: Обработка банком... + лоадер. Кнопка disabled. | Ожидание ответа от шлюза Т-Банка (/Charge или /FinishAuthorize). |
| 3. 3D-Secure | Банк вернул 3DS_CHECKING | Текст: Подтвердите по СМС. Кнопка disabled. | Открытие банковского ACSUrl внутри iframe поверх текущего экрана для ввода кода. |
| 4. Success | Банк вернул CONFIRMED | Зеленый фон, текст: Оплачено ✔. Кнопка disabled. | Фиксация стейта на 1.5 секунды, затем автоматический редирект на экран «Спасибо за покупку». |
| 5. Client Error | Отказ банка по карте юзера (коды Т-Банка: лимиты, неверный код, баланс) | Красный фон, текст: Отказ: смените карту. Кликабельна. | Кнопка «трясется» (shake-анимация). Список карт разблокируется. При тапе юзера на другую карту (например, с *5953 на *5938), кнопка сбрасывается в State 0. |
| 6. Bank Error | Ошибка шлюза банка / Backend 500 | Серый/Красный фон, текст: Сбой банка: позже. Кликабельна. | Дает пользователю возможность повторить отправку платежа без изменения параметров. |
| 7. Net Error | Пропала сеть (navigator.onLine == false) или timeout > 10 сек | Серый фон, текст: Нет сети: повторить. Кликабельна. | Кнопка разблокируется. Повторный клик запускает процесс заново с State 1. |

**Дополнительные UX-требования**

- Защита от микро-мерцания: Если бэкенд/банк отвечает быстрее, чем за 600 мс, искусственно удерживать лоадер (сумму стейтов Connecting + Processing) до 600 мс, чтобы текст в кнопке не моргал слишком быстро.
- Никаких алертов: Запрещено выводить стандартные всплывающие окна ошибок (alert, toast, modal), если они не касаются критического падения всего приложения. Вся коммуникация по платежу идет через кнопку.

**Критерии приёмки (Acceptance Criteria)**

- Поведение кнопки при различных сценариях (успех, отказ, оффлайн) строго соответствует описанной матрице состояний.
- Время реакции кнопки на клик (переход в State 1) составляет не более 100 мс.
- При ошибке карты («Отказ: смените карту») выбор любого другого сохраненного метода оплаты из списка на макете 1000008924.png мгновенно возвращает кнопку в исходное синее состояние.

**Макет:** [`1000008924.png`](../../artifacts/demo-feedback/screenshots/1000008924.png)

---

## Фаза 0 gate R3 (2026-06-24)

**Цель:** синхрон ops перед кодом R3 · без блокеров на конфликты v1/v2.

### Решения владельца (зафиксировано в сессии)

| ID | Решение | Коммит/код |
|----|---------|------------|
| **Q-R2-1** | **A)** nonPCI кастом + RSA — канон v2, iframe v1 не используем для новой карты | R1+R2+R3 `[x]` реализовано по v2 |
| **Q-R2-2** | **A)** тумблер «Использовать карту…», **default on** | `NewCardSheet.svelte` `[x]` реализовано по v2 |
| **Q-R2-3** | **A)** макеты 8924/8925 — канон UI; пиксель-перфект — приёмка заказчика после Fly MCP | R2+R3 `[x]` реализовано по v2 · Fly MCP 2026-06-24 |
| **Deploy** | **`fly deploy` после R3** | **`[x]`** владелец 2026-06-24 |
| **TBANK_RSA_PUBLIC_KEY** | Fly secrets · форма «временно недоступна» без ключа | **`[x]`** Fly 2026-06-25 · MCP step 02 PASS |
| **CardHolder** | Из **имени на checkout** | **`[x]`** `cardHolderName \|\| name` · runbook |

**Артефакт gate:** [`b112_r3_phase0_gate_2026-06-24.json`](../../artifacts/demo-feedback/b112_r3_phase0_gate_2026-06-24.json)

### Макет 8924 vs код (после фазы 1, 2026-06-24)

| Элемент макета 8924 | Статус | Фаза |
|--------------------|--------|------|
| Шторка «Способ оплаты» + закрыть (X) | **`[x]`** `PaymentMethodsSheet.svelte` | 1 |
| Список карт `Карта *5953` + бренд | **`[x]`** `GET /saved_cards` + `formatCardListLabel` | 1 |
| СБП в списке disabled | **`[x]`** строка + «Будет позже» | 1 |
| «Новая карта» + chevron | **`[x]`** → `NewCardSheet` | 1 |
| Сброс кнопки при смене карты | **`[x]`** `resetPaymentFsm` → State 0 | 1 |
| Оранжевая кнопка «Оплатить» внизу | **`[x]`** `CheckoutPayButton` в шторке (legacy стейты) | 1 |
| FSM тексты/цвета 0–7 по матрице | **`[x]`** `shopPayFsm.js` + `CheckoutPayButton` | 2 |
| 3DS iframe overlay | **`[x]`** `ThreeDsOverlay` + `THREE_DS_FRAME_NAME` | 2 |
| Anti-flicker 600 ms | **`[x]`** `withMinLoaderMs(600)` | 2 |

**Артефакт фазы 2:** [`b112_r3_phase2_fsm_2026-06-24.json`](../../artifacts/demo-feedback/b112_r3_phase2_fsm_2026-06-24.json)

**Следующий шаг:** **апрув заказчика** · опционально `fly secrets set TBANK_RSA_PUBLIC_KEY`.

---

## Фаза 3 — хвосты R2 + Fly MCP (2026-06-24)

| # | Хвост | Статус |
|---|-------|--------|
| 3.1 | `TBANK_RSA_PUBLIC_KEY` | **`[x]`** Fly secret 2026-06-25 · `card_data_ready: true` |
| 3.2 | CardHolder | **`[x]`** имя с checkout → `encryptCardPayload` · задокументировано |
| 3.3 | Q-R2-1..3 | **`[x]`** реализовано по v2 (см. gate + таблица конфликтов) |
| 3.4 | Legacy iframe | **`[x]`** checkout без `redirectToBankPayment` · `tbankPayment.js` только session resume |

**Fly MCP:** `ruby bin/b112_r3_one_click_prep_fly.rb` + `node bin/b112_r3_fsm_mcp.mjs` — **10/10 PASS** (2026-06-25).  
**Артефакт:** [`b112_r3_fsm_ops_pass_2026-06-25.json`](../../artifacts/demo-feedback/b112_r3_fsm_ops_pass_2026-06-25.json)  
**Скрины:** `screenshots/b112_r3_fsm_checkout_2026-06-25.png`, `b112_r3_fsm_after_pay_2026-06-25.png`

---

## Конфликты scope: v1 (ответы 2026-06-18/19) vs v2 (ТЗ 2026-06-24)

| ID | Было согласовано | Новый текст заказчика | Статус |
|----|------------------|----------------------|--------|
| **Q-R2-1** (ex Q1) | Только **web-iframe** Т-Банка | **Полный отказ от iframe** · кастомный UI + RSA nonPCI | **`[x]`** реализовано по v2 |
| **Q-R2-2** (ex Q5) | Сохранять **всегда**, **без галочки** | Тумблер «Использовать карту…» (**default on**) + `save_card` | **`[x]`** реализовано по v2 |
| **Q-R2-3** (ex Q6) | **Без макетов**, стиль витрины | **100%** по 8924 / 8925 | **`[x]`** реализовано по v2 · Fly MCP |

**Без изменений (действуют):** Q2 все карты · Q3 card only · Q4 verified email · Q7 ошибки (уточнить под FSM 5–7 и ErrorCode).

### Вопросы владельцу (rev2) — закрыты 2026-06-24

| # | Вопрос | **Решение** |
|---|--------|-------------|
| **Q-R2-1** | Канон ввода карты? | **A)** nonPCI + RSA (v2) |
| **Q-R2-2** | Сохранение карты? | **A)** тумблер, default on |
| **Q-R2-3** | Дизайн? | **A)** макеты 8924/8925 канон; приёмка после Fly MCP |

---

## Сверка Т-Банк nonPCI (этап 0, 2026-06-24)

Полный JSON: [`b112_tbank_nonpci_review_2026-06-24.json`](../../artifacts/demo-feedback/b112_tbank_nonpci_review_2026-06-24.json)

| Тема | Документация Т-Банка | CoffeeOS сейчас |
|------|----------------------|-----------------|
| **Charge** | Init + Charge + RebillId | **Есть** (`charge_recurrent`) |
| **FinishAuthorize** | `POST /v2/FinishAuthorize` — PaymentId + CardData | **R1 `[x]`** 2026-06-24 |
| **CardData** | RSA-2048 на клиенте (R2) | **R2 `[x]`** `NewCardSheet` + `tbankCardEncrypt.js` |
| **3DS** | `3DS_CHECKING` → ACSUrl, PaReq, MD | **R1 `[x]`** API proxy · **R3 `[x]`** FSM overlay |
| **ErrorCode** | Коды в ответе API | **R1 `[x]`** проброс + friendly для 1051/1014 |

**Закрыто R2–R3:** CardData RSA · `TBANK_RSA_PUBLIC_KEY` на Fly · CardHolder = имя checkout.

**Открыто (приёмка):** RebillId → `mobile_payment_methods` после успешной 1-й оплаты на стенде заказчика.

---

## Критерии приёмки rev2 (OPS)

| № | Критерий | R | Код | Заказчик |
|---|----------|---|-----|----------|
| 1 | `mobile_payment_methods` (= UserCards в ТЗ): token, bank_card_id, mask, exp | R1 | `[x]` | `[ ]` приёмка |
| 2 | Эндпоинт новая карта: Init + FinishAuthorize + save_card | R1 | `[x]` | `[ ]` |
| 3 | Эндпоинт 1 клик: Init + Charge по RebillId | R1 | `[x]` | `[ ]` |
| 4 | 3DS_CHECKING → ACSUrl, PaReq, MD на фронт | R1 | `[x]` | `[ ]` |
| 5 | ErrorCode банка на фронт без искажения | R1,R3 | `[x]` R1 API | `[ ]` |
| 6 | PAN/CVV не в БД и не в Network открытым текстом | R1,R2 | `[x]` | `[ ]` |
| 7 | Форма новой карты = макет 8925 | R2 | `[x]` | `[ ]` |
| 8 | RSA CardData на клиенте | R2 | `[x]` | `[ ]` |
| 9 | Luhn + маски + блок кнопки | R2 | `[x]` | `[ ]` |
| 10 | Экран способов оплаты = макет 8924 | R3 | `[x]` | `[ ]` |
| 11 | FSM состояния 0–7 по матрице | R3 | `[x]` | `[ ]` |
| 12 | Anti-flicker 600 ms · shake · сброс при смене карты | R3 | `[x]` | `[ ]` |
| 13 | Нет alert/toast по платежу | R3 | `[x]` | `[ ]` |
| 14 | Редирект успех 1.5 с | R3 | `[x]` | `[ ]` |
| 15 | Клик → State 1 ≤ 100 ms | R3 | `[x]` код | `[ ]` Fly MCP |

---

## Чеклист B1.12-R1 rev2 (после **`go` R1** — только документ 1)

- [x] Миграция/алиасы полей UserCards ↔ `mobile_payment_methods`
- [x] `TbankAdapter#finish_authorize`
- [x] Shop API: init new card (CardData, save_card)
- [x] Shop API: charge by internal card_id
- [x] Проброс 3DS_CHECKING + ErrorCode
- [x] Unit + integration тесты
- [x] Артефакт `b112_r1_nonpci_ops_pass_2026-06-24.json`
- [x] **Шаг закрыт:** commit + SESSION_STATE + CHANGELOG + HANDOFF → **стоп до `go` R2**

## Чеклист B1.12-R2 rev2 (после **`go` R2** — только документ 2; R1 `[x]`)

- [x] UI «Новая карта» по 8925
- [x] `tbankCardEncrypt.js` RSA-2048
- [x] Маски, Luhn, тумблер save_card
- [x] Network audit: нет открытого PAN/CVV
- [x] Integration / e2e тест
- [x] Артефакт `b112_r2_custom_card_ops_pass_2026-06-24.json`
- [x] **Шаг закрыт:** commit + ops → **стоп до `go` R3**

## Чеклист B1.12-R3 rev2 (после **`go` R3** — только документ 3; R1+R2 `[x]`)

- [x] UI «Способ оплаты» по 8924 (фаза 1 — `PaymentMethodsSheet`)
- [x] FSM 0–7 в кнопке (фаза 2 — `shopPayFsm.js`, `CheckoutPayButton`)
- [x] 3DS iframe overlay (State 3)
- [x] Anti-flicker 600 ms
- [x] Fly MCP прогон · артефакт `b112_r3_fsm_ops_pass_2026-06-25.json` (**10/10**)
- [ ] **Шаг закрыт:** апрув заказчика на эпик

---

## Ответы заказчика v1 (2026-06-18) — архив

Q1/Q5/Q6 заменены Q-R2-1..3 (закрыты). Q2–Q4, Q7 действуют.  
Артефакт: [`b112_customer_answers_confirmed_2026-06-19.json`](../../artifacts/demo-feedback/b112_customer_answers_confirmed_2026-06-19.json)

---

## ТЗ v1 (2026-06-18) — архив

Тексты v1 (webhook + iframe + «всегда без галочки») **не канон**. История: артефакты `b112_*` до 2026-06-24.

---

**Статус:** код R1–R3 **`[x]`** · MCP **10/10** · RSA Fly **`[x]`** · **открыто:** приёмка сохранения карты · апрув `[ ]`
