---
name: start
description: "CoffeeOS /start — умный старт сессии (шапка ops; PHASE 0 только для нового ТЗ). Вызывать через /start."
disable-model-invocation: true
---

Выполни инструкции из `.cursor/commands/start.md` целиком (умный старт A/B, отчёт, строка `Next: /…`).
