Отчёт: правила Cursor для CoffeeOS
Дата: 2026-04-04

Сделано
- Добавлен один файл .cursor/rules/coffeeos-core.mdc с alwaysApply: true.
- В нём 8 коротких пунктов: тенант/RLS, панели, enum Rails 8, миграции, секреты, объём правок, проверки, честность к схеме.

Почему так
- Один компактный rule вместо многих ролей — меньше шума в контексте, проще поддерживать.
- Жёсткие пункты совпадают с реальной архитектурой репо (мультитенант, несколько layout, без утечки секретов).

Дальше
- При необходимости добавить узкие rules с globs (например db/migrate/**) — опционально.
